# H1 Implementation Boundary

Use existing Next.js + Firebase + wallet + blockchain infrastructure only. No new API providers, external services, paid infrastructure, or transaction execution rails.

Security and UX work that cannot be safely completed inside these boundaries is deferred rather than approximated.

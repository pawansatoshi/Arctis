'use client';
import { useState,useEffect,useCallback,Suspense } from 'react';
import { useAccount,useSwitchChain,useWriteContract } from 'wagmi';
import { createViemAdapterFromProvider } from '@circle-fin/adapter-viem-v2';
import { AppKit } from '@circle-fin/app-kit';
import { ArcTestnet } from '@circle-fin/app-kit/chains';
import { createPublicClient,formatEther,http,parseUnits } from 'viem';
import { ArrowLeftRight,History,ShieldCheck,AlertCircle,Loader2 } from 'lucide-react';
import { CONTRACTS,ERC20_ABI,RPC_URL,txUrl } from '@/lib/contracts';
import { isCircleSwapPair } from '@/lib/swap/circle';
import { useAppStore } from '@/lib/store';
import { useWalletAuth } from '@/lib/auth/useWalletAuth';
import { ModeTabs,type ExecutionMode } from '@/components/agent/ModeTabs';
import { EconomicAgentPanel } from '@/components/agent/EconomicAgentPanel';
import { TransactionConfirmationModal } from '@/components/transaction/TransactionConfirmationModal';
import { announceTransactionState } from '@/lib/transaction/voice';
import { formatRelative } from '@/lib/utils';
import toast from 'react-hot-toast';

type SwapToken='USDC'|'EURC'|'cirBTC'|'tUSDC'|'tARC';const TOKENS:SwapToken[]=['USDC','EURC','cirBTC','tUSDC','tARC'];const DEC:Record<SwapToken,number>={USDC:6,EURC:6,cirBTC:8,tUSDC:6,tARC:18};const OTC:Partial<Record<SwapToken,`0x${string}`>>={USDC:CONTRACTS.USDC as `0x${string}`,tUSDC:CONTRACTS.tUSDC as `0x${string}`,tARC:CONTRACTS.tARC as `0x${string}`};const OTC_TOKENS:SwapToken[]=['USDC','tUSDC','tARC'];interface Quote{rail:'circle'|'otc';input:number;output:number;fee:number;available:boolean}interface Session{from:SwapToken;to:SwapToken;amount:string;quote:Quote|null;step:'idle'|'estimating'|'sending'|'processing'|'completed'|'error';txHash?:`0x${string}`;outbound?:string;error?:string;executing:boolean}const empty:Session={from:'USDC',to:'tUSDC',amount:'',quote:null,step:'idle',executing:false};
function otcPair(a:SwapToken,b:SwapToken){return OTC_TOKENS.includes(a)&&OTC_TOKENS.includes(b)}async function preflight(token:SwapToken,amount:string,address:`0x${string}`){const c=createPublicClient({transport:http(RPC_URL)});const req=parseUnits(amount,DEC[token]);const[native,gas,balance]=await Promise.all([c.getBalance({address}),c.getGasPrice(),c.readContract({address:OTC[token]!,abi:ERC20_ABI,functionName:'balanceOf',args:[address]})]);if(balance<req)throw new Error(`Insufficient ${token}. No token was sent.`);if(native<gas*100_000n*2n)throw new Error(`Insufficient ARC for swap gas. Approximately ${formatEther(gas*100_000n*2n)} ARC is required.`)}
function SwapPageInner(){const{isConnected,address,chainId,connector}=useAccount();const{switchChainAsync,isPending:isSwitching}=useSwitchChain();const{writeContractAsync}=useWriteContract();const{getAuthHeaders}=useWalletAuth();const{pendingAction,setPendingAction}=useAppStore();const[sessions,setSessions]=useState<{manual:Session;agent:Session}>({manual:{...empty},agent:{...empty}});const[mode,setMode]=useState<ExecutionMode>('manual');const[history,setHistory]=useState<Array<Record<string,unknown>>>([]);const[showHistory,setShowHistory]=useState(false);const[modal,setModal]=useState(false);const[modalMode,setModalMode]=useState<ExecutionMode>('manual');const s=sessions[mode];const modalSession=sessions[modalMode];const setS=useCallback((patch:Partial<Session>,target:ExecutionMode=mode)=>setSessions(prev=>({...prev,[target]:{...prev[target],...patch}})),[mode]);
useEffect(()=>{if(pendingAction?.action!=='swap')return;setMode('agent');setSessions(prev=>({...prev,agent:{...prev.agent,from:(pendingAction.fromToken as SwapToken)??prev.agent.from,to:(pendingAction.toToken as SwapToken)??prev.agent.to,amount:pendingAction.amount,quote:null,step:'idle'}}));setPendingAction(null);toast.success('Pre-filled from ARCTIS AI — review before swapping')},[pendingAction,setPendingAction]);
useEffect(()=>{if(!showHistory||!address)return;Promise.all([fetch(`/api/swap/history?wallet=${address}`).then(r=>r.json()).catch(()=>({swaps:[]})),Promise.resolve(localStorage.getItem(`arctis-swap-history:${address.toLowerCase()}`)).then(r=>r?JSON.parse(r):[]).catch(()=>[])]).then(([remote,local])=>{const all=[...(remote.swaps??[]),...(local??[])];const merged=Array.from(new Map(all.map((x:Record<string,unknown>)=>[String(x.id??x.inboundTxHash),x] as const)).values());setHistory(merged);});},[showHistory,address]);
const loadQuote=useCallback(async(target:ExecutionMode=mode)=>{const q=sessions[target];const n=Number(q.amount);if(!isConnected||!address||!connector||chainId!==5042002||!Number.isFinite(n)||n<=0||n>100000||q.from===q.to){setS({quote:null},target);return}setS({step:'estimating',error:undefined},target);try{if(isCircleSwapPair(q.from,q.to)){const provider=await connector.getProvider();const adapter=await createViemAdapterFromProvider({provider:provider as never,capabilities:{addressContext:'user-controlled',supportedChains:[ArcTestnet]}});const kit=new AppKit();const e=await kit.estimateSwap({from:{adapter,chain:'Arc_Testnet'},tokenIn:q.from,tokenOut:q.to,amountIn:n.toString(),config:{slippageBps:100}});setS({quote:{rail:'circle',input:n,output:Number(e.estimatedOutput.amount),fee:0,available:true},step:'idle'},target)}else if(otcPair(q.from,q.to)){const r=await fetch(`/api/swap/quote?from=${q.from}&to=${q.to}&amount=${q.amount}`);const d=await r.json();setS({quote:{rail:'otc',input:n,output:Number(d.outputAmount??0),fee:Number(d.fee??0),available:d.routeAvailable!==false},step:'idle',error:d.routeAvailable===false?d.error:undefined},target)}else setS({quote:null,step:'error',error:'This swap pair is not supported.'},target)}catch(e){setS({quote:null,step:'error',error:e instanceof Error?e.message:'Live swap quote unavailable.'},target)}},[sessions,isConnected,address,connector,chainId,mode,setS]);useEffect(()=>{const t=window.setTimeout(()=>void loadQuote(mode),350);return()=>window.clearTimeout(t)},[loadQuote,mode,s.amount,s.from,s.to]);
const save=useCallback((r:Record<string,unknown>)=>{if(!address)return;const key=`arctis-swap-history:${address.toLowerCase()}`;try{const old=JSON.parse(localStorage.getItem(key)||'[]');localStorage.setItem(key,JSON.stringify([r,...old.filter((x:Record<string,unknown>)=>String(x.id??x.inboundTxHash)!==String(r.id??r.inboundTxHash))].slice(0,100)))}catch{}},[address]);
const execute=useCallback(async(target:ExecutionMode)=>{const q=sessions[target];const n=Number(q.amount);if(!q.quote?.available||!isConnected||!address||!connector||chainId!==5042002)return;setS({step:'sending',executing:true,error:undefined},target);try{announceTransactionState('preflight');if(q.quote.rail==='circle'){const provider=await connector.getProvider();const adapter=await createViemAdapterFromProvider({provider:provider as never,capabilities:{addressContext:'user-controlled',supportedChains:[ArcTestnet]}});const kit=new AppKit();const e=await kit.estimateSwap({from:{adapter,chain:'Arc_Testnet'},tokenIn:q.from,tokenOut:q.to,amountIn:n.toString(),config:{slippageBps:100}});announceTransactionState('wallet_approval');const result=await kit.swap({from:{adapter,chain:'Arc_Testnet'},tokenIn:q.from,tokenOut:q.to,amountIn:n.toString(),config:{slippageBps:100,stopLimit:e.stopLimit.amount}});const hash=result.txHash as `0x${string}`;if(!/^0x[0-9a-fA-F]{64}$/.test(hash))throw new Error('Swap returned no valid transaction hash');setS({txHash:hash,outbound:hash,step:'completed',executing:false},target);save({id:hash,fromToken:q.from,toToken:q.to,inputAmount:n,outputAmount:Number(result.amountOut??e.estimatedOutput.amount),status:'confirmed',inboundTxHash:hash,outboundTxHash:hash,createdAt:new Date().toISOString(),rail:'Circle Swap',mode:target});announceTransactionState('submitted');announceTransactionState('processing');announceTransactionState('confirmed');setModalMode(target);setModal(true);return}const wallet=process.env.NEXT_PUBLIC_SWAP_WALLET_ADDRESS as `0x${string}`|undefined;if(!wallet||/^0x0{40}$/i.test(wallet))throw new Error('ARCTIS swap wallet is not configured.');await preflight(q.from,q.amount,address);const amountBig=parseUnits(n.toFixed(DEC[q.from]),DEC[q.from]);announceTransactionState('wallet_approval');const hash=await writeContractAsync({address:OTC[q.from]!,abi:ERC20_ABI,functionName:'transfer',args:[wallet,amountBig]});const client=createPublicClient({transport:http(RPC_URL)});const receipt=await client.waitForTransactionReceipt({hash});if(receipt.status!=='success')throw new Error('OTC settlement transaction failed on-chain.');setS({txHash:hash,step:'processing',executing:true},target);const headers=await getAuthHeaders();const response=await fetch('/api/swap/execute',{method:'POST',headers,body:JSON.stringify({walletAddress:address,fromToken:q.from,toToken:q.to,amount:n,inboundTxHash:hash})});const d=await response.json();if(!response.ok||!d.success)throw new Error(d.error??'ARCTIS OTC settlement failed.');setS({outbound:d.outboundTxHash??hash,step:'completed',executing:false},target);save({id:hash,fromToken:q.from,toToken:q.to,inputAmount:n,outputAmount:Number(d.outputAmount??q.quote.output),status:'confirmed',inboundTxHash:hash,outboundTxHash:d.outboundTxHash,createdAt:new Date().toISOString(),rail:'ARCTIS OTC',mode:target});announceTransactionState('submitted');announceTransactionState('processing');announceTransactionState('confirmed');setModalMode(target);setModal(true);}catch(e){setS({step:'error',executing:false,error:e instanceof Error?e.message:'Swap failed.'},target);announceTransactionState('failed')}},[sessions,isConnected,address,connector,chainId,writeContractAsync,getAuthHeaders,save,setS]);
const agentExecute=useCallback(async(p:import('@/lib/store').PendingFinancialAction)=>{if(!p.amount||!p.fromToken||!p.toToken)throw new Error('Swap proposal is incomplete.');setMode('agent');setSessions(prev=>({...prev,agent:{...prev.agent,from:p.fromToken as SwapToken,to:p.toToken as SwapToken,amount:p.amount,quote:null,step:'idle',executing:true}}))},[]);useEffect(()=>{if(mode==='agent'&&sessions.agent.executing&&sessions.agent.quote&&sessions.agent.step==='idle')void execute('agent')},[mode,sessions.agent.executing,sessions.agent.quote,sessions.agent.step,execute]);const close=()=>{setModal(false);setSessions(prev=>({...prev,[modalMode]:{...prev[modalMode],step:'idle',executing:false,quote:null,txHash:undefined,outbound:undefined,error:undefined,amount:''}}));};
return <div className="page-container max-w-lg safe-bottom"><div className="flex items-center justify-between mb-6"><div><span className="text-surface-500 text-xs font-semibold uppercase tracking-widest">Stablecoin OS</span><h1 className="text-2xl font-bold mt-1">Swap</h1><p className="text-surface-600 text-sm mt-1">Circle liquidity + ARCTIS OTC settlement</p></div><button onClick={()=>setShowHistory(v=>!v)} className="btn-ghost p-2" aria-label="Swap history"><History className="w-4 h-4"/></button></div><ModeTabs mode={mode} onChange={next=>{setMode(next);setModal(false)}}/>{mode==='agent'?<EconomicAgentPanel action="swap" onExecute={agentExecute} executionStatus={s.executing?'executing':s.step==='completed'?'success':s.step==='error'?'failed':'idle'} executionError={s.error} executionTxHash={s.outbound??s.txHash??null}/>:showHistory?<div className="space-y-2 mt-4">{history.length===0?<div className="glass-card p-8 text-center text-sm text-surface-600">No swaps found.</div>:history.map(x=><div key={String(x.id??x.inboundTxHash)} className="glass-card p-4"><div className="flex justify-between"><span className="text-sm font-semibold">{String(x.inputAmount)} {String(x.fromToken)} → {String(x.outputAmount)} {String(x.toToken)}</span><span className="text-xs text-emerald-600">{String(x.status)} · {String(x.mode??'manual')}</span></div><p className="text-xs text-surface-500 mt-1">{formatRelative(String(x.createdAt))}</p></div>)}</div>:<div className="space-y-4 mt-4"><div className="glass-card p-5 space-y-4"><div><label className="text-surface-600 text-xs font-medium">FROM</label><select value={s.from} onChange={e=>setS({from:e.target.value as SwapToken,quote:null,error:undefined})} className="input-base mt-1">{TOKENS.map(t=><option key={t}>{t}</option>)}</select></div><div className="flex justify-center"><button onClick={()=>setS({from:s.to,to:s.from,quote:null})} className="w-9 h-9 rounded-full border flex items-center justify-center" aria-label="Reverse swap"><ArrowLeftRight className="w-4 h-4"/></button></div><div><label className="text-surface-600 text-xs font-medium">TO</label><select value={s.to} onChange={e=>setS({to:e.target.value as SwapToken,quote:null,error:undefined})} className="input-base mt-1">{TOKENS.filter(t=>t!==s.from).map(t=><option key={t}>{t}</option>)}</select></div><div><label className="text-surface-600 text-xs font-medium">Amount</label><input value={s.amount} onChange={e=>setS({amount:e.target.value,quote:null,error:undefined})} type="number" min="0.000001" step="0.000001" placeholder="0.00" className="input-base mt-1"/></div></div>{s.quote&&<div className="glass-card p-4 space-y-2 text-sm"><div className="flex justify-between"><span>Rail</span><span className="font-semibold">{s.quote.rail==='circle'?'Circle App Kit':'ARCTIS OTC'}</span></div><div className="flex justify-between"><span>You pay</span><span>{s.quote.input} {s.from}</span></div><div className="flex justify-between font-semibold"><span>Estimated receive</span><span>{s.quote.output.toFixed(6)} {s.to}</span></div></div>}{s.error&&<div className="glass-card p-3 border-rose-500/20 bg-rose-500/5 text-sm text-rose-600 flex gap-2"><AlertCircle className="w-4 h-4"/>{s.error}</div>}{s.step==='estimating'&&<div className="text-center text-xs text-surface-500"><Loader2 className="w-4 h-4 animate-spin inline mr-1"/>Getting live quote…</div>}<button onClick={()=>void execute('manual')} disabled={!isConnected||chainId!==5042002||!s.quote?.available||s.executing||isSwitching} className="btn-primary w-full py-3.5 disabled:opacity-40">{s.executing?'Confirm in wallet…':'Review & Swap'}</button><div className="rounded-xl border border-violet-500/15 bg-violet-500/[.04] p-3 text-xs text-surface-600 flex gap-2"><ShieldCheck className="w-4 h-4 text-violet-600"/>Preflight → quote → wallet approval → on-chain confirmation.</div></div>}<TransactionConfirmationModal open={modal} data={{status:modalSession.step==='error'?'failed':'confirmed',amount:`${modalSession.amount} ${modalSession.from} → ${modalSession.to}`,route:`${modalSession.from} → ${modalSession.to}`,network:'Arc Testnet',txHash:modalSession.outbound??modalSession.txHash,explorerUrl:(modalSession.outbound??modalSession.txHash)?txUrl((modalSession.outbound??modalSession.txHash) as `0x${string}`):undefined,detail:'Verified on-chain when confirmed.'}} onClose={close} onNew={close}/></div>}
export default function SwapPage(){return <Suspense fallback={<div className="page-container max-w-lg flex items-center justify-center min-h-[60vh]">Loading…</div>}><SwapPageInner/></Suspense>}